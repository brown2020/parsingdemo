# Parse fixtures

Used when `PARSE_USE_FIXTURES=true` (CI gate / local without paid keys).

**Label — fixtures cannot prove:**
- Live Gemini / Google Generative AI quality or latency
- Puppeteer HTML→PDF render fidelity
- pdf-parse behavior on malformed or encrypted PDFs
- HEIC conversion via heic2any in headless CI
